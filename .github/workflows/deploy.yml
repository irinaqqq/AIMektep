name: CI/CD

on:
  push:
    branches:
      - main

jobs:
  lint:
    name: Lint & Typecheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with: { python-version: "3.11" }
      - name: Install linters
        run: pip install black flake8 mypy || true
      - name: Run linters
        run: |
          black --check . || true
          flake8 || true
          mypy . || true

  test:
    name: Unit tests
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      # <-- гарантируем, что корень репозитория в PYTHONPATH -->
      - name: Ensure repo root is in PYTHONPATH
        run: echo "PYTHONPATH=${{ github.workspace }}" >> $GITHUB_ENV

      - name: Install dependencies & test deps
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          # Если проект оформлен как пакет, установим в editable режиме (необязательно)
          if [ -f setup.py ] || [ -f pyproject.toml ]; then pip install -e .; fi
          pip install pytest pytest-asyncio httpx

      - name: Run tests
        run: pytest -q

  deploy:
    runs-on: ubuntu-latest
    needs: test

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_KEY }}
          script: |
            set -e
            echo "🚀 Connecting to server and deploying AIMektep..."

            # Создаём папку проекта, если её нет
            if [ ! -d "/home/${{ secrets.SERVER_USER }}/AIMektep" ]; then
              echo "📁 Cloning repo for the first time..."
              git clone https://${{ secrets.TOKEN_DEPLOY }}@github.com/irinaqqq/AIMektep.git /home/${{ secrets.SERVER_USER }}/AIMektep
            fi

            cd /home/${{ secrets.SERVER_USER }}/AIMektep

            echo "🧭 Pulling latest changes..."
            git pull origin main

            echo "🔧 Building and restarting container..."
            docker compose down
            docker compose up -d --build

            echo "✅ Deployment completed successfully!"
